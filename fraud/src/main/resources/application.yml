server:
  port: 8081

spring:
  application:
    name: fraud
  datasource:
    username: 'vladikshk'
    url: jdbc:postgresql://localhost:54320/fraud
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'